# GitHub .env Scanner — Environment Variables

# GitHub Personal Access Token
# Required for: Code Search API (unauthenticated = 10 req/min, authenticated = 30 req/min)
# Minimum scopes needed: public_repo (read-only)
# Generate at: https://github.com/settings/tokens
GITHUB_TOKEN=your_github_pat_here
